<% AppSection.all.each do |s| %>
  <%= link_to "#{ icon('asterisk') if @navigator.context == 'home' && s.label == 'DO' }#{ s.label }#{ icon('caret-right') if @navigator.context == s.slug }".html_safe, navigator_context_path(context_name: s.slug) %>
<% end %>
