<%= form_for thought, url: create_path, method: "post", html: { role: "form" } do |f| %>
  <div class="form-group">
    <%= f.text_area :content, size: "60x5", placeholder: "example: This is too hard, I'll never figure it out OR Last time I did this, I had a good time", required: true %>
  </div>

  <div class="form-group">
    <label class="radio-inline">
      This thought is:
    </label>
    <% thought.class::EFFECTS.each do |effect| %>
      <label class="radio-inline">
        <%= f.radio_button :effect, effect, required: true %> <%= effect %>
      </label>
    <% end %>
  </div>

  <button type="submit" class="btn btn-default">Continue</button>
<% end %>
