participant1:
  email: participant1@example.com
  encrypted_password: <%= Participant.new.send(:password_digest, 'secrets!') %>

participant2:
  email: participant2@example.com
  encrypted_password: <%= Participant.new.send(:password_digest, 'secrets!') %>

inactive_participant:
  email: inactive_participant@example.com
  encrypted_password: <%= Participant.new.send(:password_digest, 'secrets!') %>

active_participant:
  email: active_participant@example.com
  encrypted_password: <%= Participant.new.send(:password_digest, 'secrets!') %>